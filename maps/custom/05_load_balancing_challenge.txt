# Custom Level 5: Load balancing challenge with dynamic pressure points
# Requires intelligent distribution to avoid congestion in shared resources
nb_drones: 11

start_hub: start 0 0 [color=green max_drones=11]

# === Distribution layer ===
hub: dist_north 1 2 [zone=priority color=cyan max_drones=4]
hub: dist_middle 1 0 [color=blue max_drones=4]
hub: dist_south 1 -2 [zone=restricted color=orange max_drones=4]

# === Processing layer with shared bottleneck ===
hub: process_a 2 2 [color=blue max_drones=3]
hub: process_b 2 1 [color=blue max_drones=3]
hub: process_c 2 0 [color=blue max_drones=3]
hub: process_d 2 -1 [color=blue max_drones=3]
hub: process_e 2 -2 [color=blue max_drones=3]

# === Shared resources (pressure points) ===
hub: shared_resource_1 3 1 [color=red max_drones=2]
hub: shared_resource_2 3 -1 [color=red max_drones=2]

# === Bypass high-priority track ===
hub: priority_bypass 3 2 [zone=priority color=lime max_drones=2]
hub: priority_merge 4 2 [zone=priority color=lime max_drones=2]

# === Assembly/merge zones ===
hub: merge_upper 4 1 [color=yellow max_drones=3]
hub: merge_lower 4 -1 [color=yellow max_drones=3]
hub: merge_final 4 0 [color=orange max_drones=5]

end_hub: goal 5 0 [color=gold max_drones=11]

# Initial distribution
connection: start-dist_north [max_link_capacity=3]
connection: start-dist_middle [max_link_capacity=4]
connection: start-dist_south [max_link_capacity=3]

# Distribution to processing
connection: dist_north-process_a [max_link_capacity=2]
connection: dist_north-process_b [max_link_capacity=2]
connection: dist_middle-process_b [max_link_capacity=2]
connection: dist_middle-process_c [max_link_capacity=2]
connection: dist_middle-process_d [max_link_capacity=2]
connection: dist_south-process_d [max_link_capacity=2]
connection: dist_south-process_e [max_link_capacity=2]

# Processing layer interconnections
connection: process_a-process_b [max_link_capacity=1]
connection: process_b-process_c [max_link_capacity=1]
connection: process_c-process_d [max_link_capacity=1]
connection: process_d-process_e [max_link_capacity=1]

# Routes to shared resources
connection: process_a-priority_bypass [max_link_capacity=2]
connection: process_b-shared_resource_1 [max_link_capacity=2]
connection: process_c-shared_resource_1 [max_link_capacity=1]
connection: process_d-shared_resource_2 [max_link_capacity=2]
connection: process_c-shared_resource_2 [max_link_capacity=1]
connection: process_e-shared_resource_2 [max_link_capacity=1]

# Priority bypass route
connection: priority_bypass-priority_merge [max_link_capacity=2]
connection: priority_merge-merge_final [max_link_capacity=2]

# Shared resources merging
connection: shared_resource_1-merge_upper [max_link_capacity=2]
connection: shared_resource_2-merge_lower [max_link_capacity=2]
connection: shared_resource_1-shared_resource_2 [max_link_capacity=1]

# Final merge points
connection: merge_upper-merge_final [max_link_capacity=2]
connection: merge_lower-merge_final [max_link_capacity=2]

# Goal
connection: merge_final-goal
